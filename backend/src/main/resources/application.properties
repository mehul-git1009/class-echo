spring.application.name=class-echo-backend
server.port=8000

# Database Configuration (Supabase PostgreSQL)

#Railway Development 
spring.datasource.url=jdbc:postgresql://db.sdzspwqmlecnzkhlycvk.supabase.co:5432/postgres?sslmode=require

#Local Development
# spring.datasource.url=jdbc:postgresql://db.sdzspwqmlecnzkhlycvk.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=classecho
spring.datasource.driver-class-name=org.postgresql.Driver

# Hikari Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Supabase Configuration
supabase.url=${SUPABASE_URL:https://sdzspwqmlecnzkhlycvk.supabase.co}
supabase.service-role-key=${SUPABASE_SERVICE_ROLE_KEY}

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.token-expiration=${JWT_TOKEN_EXPIRATION}
